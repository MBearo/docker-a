version: '3'
services:
  docker-a:
    image: docker-a:latest
    ports:
      - "3000:3000"
    volumes:
      - "../public:/public"
  docker-b:
    image: docker-b:latest
    links:
      - docker-a
    volumes:
      - "../public:/public"
